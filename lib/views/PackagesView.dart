import 'package:flutter/material.dart';

class PackagesView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Packages View')),
      body: Center(
        child: Text('Welkom op de Packages Page!'),
      ),
    );
  }
}
